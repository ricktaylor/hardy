[package]
name = "hardy-proto"
version = "0.1.0"
edition.workspace = true

[lib]
path = "lib.rs"
crate-type = ["rlib"]

[dependencies]
hardy-bpa = { path = "../bpa" }
prost = "0.14"
prost-types = "0.13"
tonic = "0.13.1"

[build-dependencies]
tonic-build = "0.13.1"
