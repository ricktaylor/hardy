syntax = "proto3";

package bpa;

service cla_sink {
    rpc Receive(ReceiveBundle) returns (ReceiveResponse);
    rpc Poll(PollRequest) returns (ForwardBundle);
}

message ReceiveBundle {
}

message ReceiveResponse {
}

message PollRequest {
    string ClaName = 1;
}

message ForwardBundle {

}