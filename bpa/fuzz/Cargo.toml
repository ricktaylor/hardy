[package]
name = "hardy-bpa-fuzz"
version = "0.0.0"
publish = false
edition.workspace = true
rust-version.workspace = true

[package.metadata]
cargo-fuzz = true

[features]
default = []
postgres-storage = ["dep:hardy-postgres-storage"]
sqlite-storage = ["dep:hardy-sqlite-storage"]
localdisk-storage = ["dep:hardy-localdisk-storage"]
file-cla = ["dep:hardy-file-cla"]
instrument = ["hardy-bpa/instrument","hardy-sqlite-storage?/instrument","hardy-localdisk-storage?/instrument","hardy-file-cla?/instrument"]

[dependencies]
libfuzzer-sys = "0.4"
hardy-otel = { version = "0.1", path = "../../otel" }
hardy-async = { version = "0.1", path = "../../async" }
hardy-bpv7 = { version = "0.5", path = "../../bpv7" }
hardy-bpa = { version = "0.1", path = ".." }
hardy-postgres-storage = { version = "0.1", path = "../../postgres-storage", optional = true }
hardy-sqlite-storage = { version = "0.5", path = "../../sqlite-storage", optional = true }
hardy-localdisk-storage = { version = "0.5", path = "../../localdisk-storage", optional = true }
hardy-file-cla = { version = "0.1", path = "../../file-cla", optional = true }
tokio = { version = "1", features = ["rt-multi-thread", "time"] }
time = { version = "0.3", features = ["parsing"] }
tracing = { version = "0.1", default-features = false }
arbitrary = { version = "1", features = ["derive"] }
cfg-if = "1"
flume = "0.12"
percent-encoding = { version = "2", default-features = false, features = ["alloc"] }

[lib]
path = "src/lib.rs"
test = true
doc = false

[[bin]]
name = "bpa"
path = "fuzz_targets/bpa.rs"
test = false
bench = false
doc = false
