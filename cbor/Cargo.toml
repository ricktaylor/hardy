[package]
name = "hardy-cbor"
version = "0.1.0"
edition.workspace = true

[lib]
path = "src/lib.rs"
crate-type = ["rlib"]

[dependencies]
thiserror = { version = "2.0.17", default-features = false }
half = { version = "2.7.0", default-features = false, features = ["num-traits"] }
num-traits = { version = "0.2.19", default-features = false }

[dev-dependencies]
hex-literal = "1.0.0"
